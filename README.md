## Grep in Python (sort of)

> A lightweight and fairly basic implementation of Unix/Linux's `grep` tool built with standard libraries using Python.

### Usage

```bash
echo -n "<input-text>" | ./run.sh -E "<pattern>"
```
